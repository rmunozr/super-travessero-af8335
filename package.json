{
  "name": "deep-finance-lab-assistant",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "node --test test/*.test.js",
    "setup:sheets": "node scripts/setup-sheets.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@netlify/blobs": "^10.7.13",
    "googleapis": "^178.0.0",
    "openai": "^6.0.0"
  }
}
